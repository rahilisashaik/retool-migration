{
  "permissions": {
    "allow": [
      "Exec(npx)",
      "Exec(npm init)",
      "Exec(npm install)",
      "Exec(mkdir)",
      "Exec(docker-compose up)",
      "Exec(npm run)",
      "Exec(curl)",
      "Exec(jq)"
    ]
  }
}